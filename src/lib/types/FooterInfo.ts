export type FooterInfo = {
    title: string;
    imageUrl: string,
    creator: string,
    lengthInSeconds: number,
    atSeconds: number
    liked: boolean,
    volume: number,
    shuffle: boolean,
    playing: boolean,
    repeat: boolean,
}